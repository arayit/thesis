# latexmk configuration — used for local builds: `latexmk -pdf main.tex`
$pdf_mode = 1;          # build a PDF with pdflatex
$bibtex_use = 2;        # run biber/bibtex as needed
$clean_ext = 'bbl run.xml synctex.gz';

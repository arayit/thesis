%==============================================================================
%  CUSTOM COMMANDS  —  add your own shortcuts here.
%==============================================================================

% A horizontally and vertically centered page (used by the title page).
\newcommand{\thesisvfill}{\vspace*{\fill}}

% Common math / EE shortcuts (examples — extend as you like)
\newcommand{\R}{\mathbb{R}}
\newcommand{\E}{\mathbb{E}}
\DeclareMathOperator{\sinc}{sinc}

% Chemical formulae (text mode)
\newcommand{\nto}{N\textsubscript{2}O}
\newcommand{\cotwo}{CO\textsubscript{2}}
\newcommand{\water}{H\textsubscript{2}O}

% Non-SI units used in this work
\DeclareSIUnit{\torr}{Torr}
\DeclareSIUnit{\sccm}{sccm}
\DeclareSIUnit{\ppm}{ppm}
\DeclareSIUnit{\ppb}{ppb}

% A todo note that stands out while drafting. Comment out the body before
% your final submission, or wrap with \ifdraft logic if you prefer.
\newcommand{\todo}[1]{\textcolor{red}{\textbf{[TODO: #1]}}}

% A "citation needed" flag, visually distinct from \todo. Each one names the
% claim and the kind of source that should support it, so open citation slots
% are easy to find (grep '\needcite') and fill from trusted references.
% Never replaced by an invented \cite key. Remove all before submission.
\newcommand{\needcite}[1]{\textcolor{blue}{\textbf{[CITE: #1]}}}
